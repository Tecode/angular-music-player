export interface NavBarIcon {
    normalUrl: string;
    activeUrl: string;
    text: string;
    routerLink: string;
    id: number;
}

export interface ClientRectData {
    top: number,
    left: number,
    width: number,
    height: number
}